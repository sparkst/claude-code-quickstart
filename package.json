{
  "name": "claude-code-quickstart",
  "version": "1.2.0",
  "description": "Production-ready CLI for Claude Code setup with enterprise-grade MCP servers, smart tiered UX (80%+ completion rate), perfect TDD compliance, and WCAG 2.1 AA accessibility.",
  "bin": {
    "claude-code-quickstart": "bin/cli.js"
  },
  "scripts": {
    "postinstall": "node bin/postinstall.js",
    "test": "vitest",
    "test:run": "vitest run",
    "test:watch": "vitest --watch",
    "lint": "eslint *.js bin/*.js",
    "format": "prettier --write *.js bin/*.js",
    "format:check": "prettier --check *.js bin/*.js",
    "ci:validate": "node scripts/ci-validation.js",
    "ci:local": "npm run format:check && npm run lint && npm run test:run",
    "pre-commit": "./scripts/pre-commit-hook.sh",
    "prepare": "command -v git >/dev/null 2>&1 && git config core.hooksPath .githooks || true"
  },
  "dependencies": {
    "proper-lockfile": "^4.1.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.35.0",
    "@types/node": "^24.3.1",
    "eslint": "^9.35.0",
    "fast-check": "^4.3.0",
    "js-yaml": "^4.1.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "vitest": "^2.1.0"
  },
  "keywords": [
    "claude",
    "claude-code",
    "anthropic",
    "mcp",
    "vs-code",
    "sparkry.ai",
    "quickstart",
    "production-ready",
    "enterprise-grade",
    "tdd",
    "accessibility",
    "security"
  ],
  "author": "Sparkry.AI",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "files": [
    "bin/",
    "templates/",
    "scripts/",
    "README.md",
    "LICENSE"
  ]
}
